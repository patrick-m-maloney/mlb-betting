[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python src/scripts/mlb_scraper_bot.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5

[nixpacks]
# Use Python 3.12 — 3.13 has known issues with some scientific packages
pythonVersion = "3.12"
